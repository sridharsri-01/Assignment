create table shopping (
    product_id int primary key,
    product_name varchar(50),
    price decimal(10,2)
);

insert into shopping values
(1, 'laptop', 55000.00),
(2, 'mobile', 25000.00),
(3, 'headphone', 2000.00),
(4, 'smartwatch', 5000.00),
(5, 'tablet', 30000.00);

select product_name, price from shopping where price = (select max(price) from shopping);
select product_name, price from shopping where price = (select min(price) from shopping);
