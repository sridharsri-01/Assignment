create table shops(
    product_id int primary key,
    product_name varchar(50),
    price decimal(10,2)
);

insert into shops values
(1,'lap',55000),
(2,'mobile',25000),
(3,'phone',2000),
(4,'watch',5000);
create table orders (
    product_id int,
    order_status varchar(20),
    shipping_detail varchar(50)
);

insert into orders values
(1,'delivered','chennai'),
(2,'shipped','madurai'),
(5,'pending','coimbatore');

select s.product_id, s.product_name, s.price,  o.order_status, o.shipping_detail from shops s
inner join orders o on s.product_id = o.product_id;

select s.product_id, s.product_name, s.price,o.order_status, o.shipping_detail from shops s
left join orders o on s.product_id = o.product_id;

select s.product_id, s.product_name, s.price, o.order_status, o.shipping_detail from shops s
right join orders o on s.product_id = o.product_id;

select s.product_id, s.product_name, s.price, o.order_status, o.shipping_detail from shops s
left join orders o on s.product_id = o.product_id

union

select s.product_id, s.product_name, s.price, o.order_status, o.shipping_detail from shops s
right join orders o on s.product_id = o.product_id;
